#ifndef DATABASE_FILENAMES_H
#define DATABASE_FILENAMES_H

#include <string>

namespace MessageQueueInfo {
    const std::string FILE = "/bin/bash";
    const char LETTER = 'A';
}

namespace DatabaseInfo {
    const std::string DB_FILE = "/tmp/db";
}

#endif //DATABASE_FILENAMES_H